import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { lazy, Suspense, useEffect } from 'react';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route />
        <Route />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
