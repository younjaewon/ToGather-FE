import styled from '@emotion/styled';

const MyProjectBlock = styled.div`
  position: relative;
  top: 5rem;
  padding: 0 4rem;
`;

const Wrapper = styled.div`
  max-width: 1180px;
  margin: 0 auto;
`;

export { MyProjectBlock, Wrapper };
